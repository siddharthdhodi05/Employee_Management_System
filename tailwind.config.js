/** @type {import('tailwindcss').Config} */
export default {
   content: [
    './index.html', // Add this line to include your HTML files
    './src/**/*.{js,ts}', // This will include JS/TS files in the src folder (if any)
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

